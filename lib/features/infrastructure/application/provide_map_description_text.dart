import 'package:flutter/material.dart';

class ProvideMapDescrptionText extends StatelessWidget {
  const ProvideMapDescrptionText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
        "In the map screen you can set a point on the map. To do so, first activate the"
        "function to set a geopoint");
  }
}
