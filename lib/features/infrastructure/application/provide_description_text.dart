import 'package:flutter/material.dart';

class ProvideDescriptionText extends StatelessWidget {
  const ProvideDescriptionText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
        "The 4H App is about sending a Signal, so that you can get help from other fellows nearby."
        "You can use the Map to set a specific point or you can just set a signal from your current position.");
  }
}
